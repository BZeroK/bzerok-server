package com.bzerok.server.domain.liquorCategories;

import org.springframework.data.jpa.repository.JpaRepository;

public interface LiquorCategoriesRepository extends JpaRepository<LiquorCategories, Long> {
}
