server.address=localhost
server.port=8080

# API 호출시 SQL 문을 콘솔에 출력
spring.jpa.show-sql=true

# DDL 정의시 데이터베이스의 고유 기능 사용
spring.jpa.generate-ddl=true

# MySQL 사용
spring.jpa.database=mysql

# MySQL 설정
spring.datasource.url=jdbc:mysql://localhost:3306/bzerok_db?useSSL=false&characterEncoding=UTF-8&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
spring.datasource.username=bzerok
spring.datasource.password=dud41272201
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# MySQL 상세 지정
spring.jpa.database-platform=org.hibernate.dialect.MySQL5InnoDBDialect